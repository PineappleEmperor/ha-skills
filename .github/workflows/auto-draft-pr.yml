name: Draft PR

on:
  push:
    branches-ignore: [main]

permissions:
  contents: read

jobs:
  draft:
    uses: PineappleEmperor/release-flow/.github/workflows/auto-draft-pr.yml@87305e56bf921fe8728a2b448b112b06da6cc082 # v1.1.1
    secrets:
      release-token: ${{ secrets.RELEASE_TOKEN }}
