# Test-only dependencies. Installed by ha-integration-ci's python-validate.yml
# before the pytest step; also `pip install -r requirements.test.txt` locally.
#
# The pin below decides which Home Assistant release the suite runs against; why,
# and how it must track the Python floor the CI declares, is the ha-integration
# skill's reference/testing.md. The audit compares the two.
#
# Which HA release a given pin carries, and its Python floor, is on
# pypi.org/project/pytest-homeassistant-custom-component. No mapping is written
# here on purpose: Dependabot moves the pin and never a comment beside it.
pytest-homeassistant-custom-component==0.13.365


# Add integration-specific test deps below (e.g. a fake transport, respx,
# freezegun beyond what the plugin already pins).

# --- Panel integrations only -------------------------------------------------
# A panel declares `frontend` in manifest.json `dependencies`, and the frontend
# component's own pip package is not pulled in by `pip install homeassistant`;
# without the pin below every setup test fails in CI while passing locally. Why,
# and how to pin it from core's own manifest for the HA release pinned above, is
# the ha-integration skill's reference/panels.md. Re-derive it whenever the
# pytest-homeassistant-custom-component pin moves.
# home-assistant-frontend==<version from core's frontend manifest>   # uncomment for panel integrations
