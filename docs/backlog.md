# Backlog

Problems, not tasks. A row records what is wrong. The decision recorded against it is the
current best answer and may be replaced when new information arrives. A row closes when the
problem is gone, not when the fix once planned for it is done, and being in this file is
never a reason to finish work that has been overtaken.

Nothing here is edited during an audit: an audit adds rows, a later pass clears them, one
issue per commit. A finding discovered while fixing is added here rather than fixed inline.

Status: `open` · `decided` · `fixed` (with commit) · `superseded by <row>` · `wontfix` (with reason)

Live rows: 98, 159, 163, 168, 171, 172, 177 and 182. Rows 178 to 181 closed on 2026-09-12 and sit
in their section beside 177. Rows 143, 144 and 149 left on 2026-09-11 for the
repositories they describe; rows 173 to 176 closed on 2026-09-10 and sit in the section they
were raised in until it sheds, since 172 still holds it open. Every other row has
closed into a file under `docs/backlog/`, named for the phase it belongs to and linked from
the section it left. Numbers are globally unique and never reused, because commit messages
in five repositories address them.

Reading a Commit column: rows 100-102, 104-115, 120, 121, 129, 130 and 133 name the first
commit of the repository where the fix lives rather than a commit in this one, and rows
from 134 name that repository's later commits by hash. Without that, a bare hash in a phase
file resolves against nothing.

A closed row is not rewritten. A claim in one that turns out to be false becomes a new row
here, so the phase files stay a transcript that can be checked against git. Live rows take
the problem, decision and evidence shape as they are next worked, rather than in one pass:
rewriting a row nobody is touching is how this file drifted out of step with itself twice
in a day.

### From the reviews of the three new repositories (2026-09-05)

Moved to `docs/backlog/2026-09-05-three-new-repos.md`. Rows 100 to 116.

### From the single-source audit of 2026-09-05

Moved to `docs/backlog/2026-09-05-single-source.md`. Rows 117 to 128.

### From the fourth single-source review (2026-09-05, after rows 117-127 cleared)

Moved to `docs/backlog/2026-09-05-fourth-review.md`. Rows 129 to 133.

### From the v1.0.0rc1 cycle of the three CI repositories (2026-09-05)

Moved to `docs/backlog/2026-09-05-rc1-cycle.md`. Rows 134 to 140.

### From the audit of the two integrations still to migrate (2026-09-06)

`skill_audit.py` at ha-integration-ci `v1.0.0` run against clones of `ha-lego` (54 failures)
and `ha-pimoroni-unicorn` (53); with rows 141 and 142 fixed, 52 and 53. Most are the
pre-caller state row 98 replaces and are not rows of their own. These are the ones that
are not: two failures the audit reports wrongly, and five facts about the repositories and
the stack that the migration has to answer before it starts.

Rows 141, 142, 145, 146 and 147 moved to `docs/backlog/2026-09-06-integrations-audit.md`.

Rows 143, 144 and 149 left this register on 2026-09-11. Each described `ha-lego` or
`ha-pimoroni-unicorn` rather than this repository, and each is already recorded in the
repository it belongs to, in its own `docs/ci-migration-todo.md`: 143 under *`main` is not
protected*, with the ruleset id, the bypass and the context names; 144 under *Live now, and
fixed by the migration*; 149 in both, in the deletion list under the workflow table. This
register tracks writing the skill. A problem in a repository that consumes it is that
repository's, and holding a copy here is what made this branch read as blocked on work it
does not own.

### From proving the panel path on the testbed (2026-09-06 to 2026-09-07)

Every row here was found by doing the work rather than by reading it: applying the
rulesets, giving the testbed a panel, building the path check against a reviewer's
counter-examples, and reviewing each branch that came out of it.

Rows 148, 150, 151, 152, 153, 154, 155, 156, 157 and 158 moved to `docs/backlog/2026-09-06-panel-path.md`.

| # | Finding | Fix | Commit |
|---|---|---|---|
| 159 | **Neither panel defect needed a testbed to find, and one of them was mine to catch.** Row 157 trips only on `npm ci`; `npm install` writes the faulty lock file and exits 0. All four commands were run locally before pushing, but the install was run as `npm install`, which is not the command the workflow runs; `npm ci` is the one that reads the lock file back, and it was never run, so a lock file nobody could see was wrong reached a runner. Row 156 is older: the shipped `.gitignore` never covered `node_modules`, `ha-lego` hit that and added the line to *its own* copy, and nothing carries a consumer's fix to a copied file back to the template it was copied from. Row 98 solved that one-way street for workflow bodies and left it open for `.gitignore`, `pyproject.toml`, `conftest.py`, `ruleset.json` and the drafter config | For 157: ha-panel-ci's README now carries the four commands in order, install first, under *Proving a panel locally*, and the claim that it "fails only in CI" is corrected — it fails anywhere `npm ci` runs. For 156 the one-way street has no fix yet and is the open half of this row. **Shaped 2026-09-10 by row 173's re-decision:** the fix is not a template comparison, which that row measures as unworkable, but a content check in `skill_audit.py` of the kind the audit already uses — a repository carrying `frontend/` must ignore `node_modules/`. It needs no template, no pin and no diff, and it catches the case in every consumer rather than only where someone happened to look. The general one-way street it belongs to stays open: a consumer's improvement to a copied file still reaches the template only when a person carries it | `ha-panel-ci` 7ff2c39; the content check open |

### From readying the branch for merge (2026-09-08)

| # | Finding | Fix | Commit |
|---|---|---|---|
| 173 | **Striking goal (7) of row 98 removed the only thing that would have surfaced row 156.** Goal (7) had the audit compare what a scaffold still copies — `.gitignore`, `pyproject.toml`, `conftest.py`, `ruleset.json`, the drafter config, the commit hook — against the templates at the pinned version, and name the drift. It was struck on 2026-09-06 on the grounds that `check_callers`, `check_action_pins` and the config checks cover what matters. Row 156 is the counter-example, and it arrived after the strike: `ha-lego` had added `node_modules/` to its own copy of the template's ignore file, the template never learned of it, and it was found only because a panel install in the testbed offered the whole tree for staging. Row 159's open half is the same shape stated as a rule, and it currently has no path to a fix, because the check that would have reported the difference is the one that was struck. Goal (7) and row 159 point in opposite directions — one detects a consumer diverging from the template, the other carries a consumer's improvement back — but the detection is the first half of the second, and neither exists now | **Re-decided 2026-09-10: the strike stands, and this row's premise was wrong.** Three findings, in the order they settle it. First, the decision already had recorded reasoning nobody consulted: ha-integration-ci's README, under *What the audit deliberately no longer checks*, lists `check_template_pins` — "compared action pins in templates against Dependabot-bumped copies" — as deliberately deleted, because a consumer's pin moving ahead of the template is correct rather than drift. That is goal (7) almost exactly. Second, the reason it cannot be built is absent from row 98's strike note and is stronger than the reason given there: `skill_audit.py` runs from ha-integration-ci's checkout, which carries no `templates/` by design, and a scaffold pins the three CI repos by SHA but has no pin to this repository at all, because a Claude Code marketplace declares no version. "Compared against the templates at the pinned version" therefore names two things that do not exist. Third, measured against `ha-lego` on 2026-09-10, five of six copied files differ: `.gitignore` 18 diff lines against a 26-line template, `conftest.py` 18 against 24, `ruleset.json` 35 against 55, `requirements.test.txt` 49 against 39, and `pyproject.toml` 276 against 237 — more different than similar. Only `.github/dependabot.yml` matches. That is an integration carrying its own dependencies and its own ruff tables, not drift, so the check would fire on almost every file for every consumer and bury its one real signal. Row 156 is not evidence for a whole-file comparison; it is evidence for a content check of the kind the audit already uses throughout — a repository carrying `frontend/` must ignore `node_modules/` — which needs no template, no pin and no diff. That check is row 159's remaining work | wontfix, decision stands |
| 172 | **`Version validation` fails on PR #65 with a traceback, not a verdict.** `ValueError: cannot parse version: 'null'` out of `manifest_gate.py`. The job runs from `main`'s copied `pr-checks.yml` under `pull_request_target`, reads the branch's `plugins/ha/.claude-plugin/plugin.json`, and finds no version, because a Claude Code marketplace declares none and Claude Code resolves it from the commit SHA. Proven with the diff, which is what the narrow exception in `discipline.md` requires before it may be claimed: `main`'s copy defines the job and carries `7.3.0rc2`; the branch's `pr-checks.yml` is a caller with one `pr` job and no version gate, and its manifest carries no version at all. So the PR deletes both the job and the data the job reads, and the job can never go green on the PR that removes it. Ruleset 18081788 does not require this context, so it does not block the merge | Nothing on the branch. This is the one sanctioned `pull_request_target` case, and it disappears at the merge along with the copied body; whoever merges says so in the PR, as that rule demands, and verifies on the next PR. The crash itself is confined to the copy being deleted: release-flow's `pr-checks.yml` calls `manifest_gate.py --suggest` with `--last-release` and `--labels` only, never `--pr-version`, so the `parse_semver` call that raised is unreachable in the shipped model, and `--last-release` falls back to `0.0.0` when no full release exists | open, resolves at merge |

### From draining the merge queue (2026-09-10)

| # | Finding | Fix | Commit |
|---|---|---|---|
| 174 | **Every CI repository requires its checks to have run against an up-to-date branch, and none of them allows auto-merge, so each merge invalidates every other open PR.** All three rulesets carry `strict_required_status_checks_policy: true`; all three repositories report `allow_auto_merge=false`. The visible symptom is a merge refused with `4 of 4 required status checks are expected` on a PR whose four checks are all green on its head SHA — GitHub is not reporting a failure, it is declining to count checks that did not run against the current base. Nine PRs were queued on 2026-09-10 as if they were independent; they are not, and merging any one of them re-blocks the rest. Three of the tools that would resolve this are unavailable here: `gh pr merge` and `gh api -X PUT` (so `pulls/N/update-branch`) are denied by `.claude/settings.local.json`, and the `PATCH` that would set `allow_auto_merge` is refused by the sandbox classifier. Separately, the workaround has its own hazard: bringing a branch up to date with `git merge` writes any governed file the base moved, which the gate correctly refuses, so the merge must instead be built with `git merge-tree` and `git commit-tree` and installed with `git update-ref`. That leaves the clone's worktree at the old content while `HEAD` carries the new — the drift row 167 describes, arrived at by a different route. It is only safe on a branch that is not the one checked out | Enable `allow_auto_merge` on the three repositories and queue each PR with `--auto`, so GitHub serialises the cascade rather than a person re-running it once per merge. Until then the plumbing route is what unstales a branch, and it must be used only on branches the clone does not have checked out; the worktree it leaves behind can only be repaired from a shell the sandbox does not cover, since every drifted path is governed. Proven on 2026-09-10: `release-flow` #3 and `ha-ci-testing` #17 and #18 were unstaled this way and #3 and #17 merged, while the `release-flow` clone was left with four files behind `HEAD` and could not restore them itself. **Done 2026-09-10:** `allow_auto_merge` and `allow_update_branch` are both true on all three repositories, and the nine open PRs drained the same evening | fixed |
| 175 | **No governed clone can be brought up to date from inside the sandbox, including by `git pull`.** Updating a branch makes git unlink the old file before writing the new one, and every governed path is read-only, so a plain fast-forward fails the moment `main` has moved anything under `scripts/`, `tests/`, `.github/workflows/` or `README.md`. Observed 2026-09-10 on `ha-integration-ci`, clean and on `main`, five commits behind: `git merge --ff-only origin/main` reported `unable to unlink old 'scripts/skill_audit.py': Read-only file system` for two paths and `Device or resource busy` for `README.md`, and left the branch behind. This is not the plumbing hazard of row 174, which is about a branch pointer moving without the worktree; it is the ordinary act of syncing a clone, and it is blocked for every one of the five governed repositories. The consequence is that any work needing a current clone stalls until the maintainer runs one command, and a commit made on a stale clone is rejected as a non-fast-forward rather than silently landing | Not the gate malfunctioning: the same read-only rule that stops an unreviewed edit stops the unlink. Two things follow. Syncing a clone is the maintainer's, like the pushes in `[push-auth]`, and belongs in the same handover rather than being discovered each time. And a clone must be confirmed current before any file is created in it, since the gate will happily create a new file on a stale base and the push then fails. Cloning elsewhere to escape the read-only paths is the one fix that must not be used — it is routing around the gate, which is what the gate exists to prevent | rule, not code |
| 176 | **Both defects row 171 recorded recurred the moment new prose was written, on the same day.** A sixth context-free reviewer, run over the three unpushed CI-repo branches before handover, found them. Restatement instead of pointing: `ha-integration-ci`'s README gained a paragraph that says what the coverage check does one sentence after pointing at release-flow's README, which already says it at `README.md:98-100` and `:104`, and repeats the check-run name that the same file already points at under Check names; `ha-panel-ci`'s gained a trailing clause repeating the `v1.0.0` anecdote from `release-flow/README.md:101-102`. A subject overstating its diff: `bb40c04` reads `ci: bump the panel dependencies on the testbed`, and the diff bumps nothing — it adds an `npm` ecosystem entry so Dependabot will open bump PRs later. Row 171 recorded both classes on 2026-09-08 and neither was avoided on 2026-09-10, which means recording a class is not what stops it; a reviewer reading the diff is. A third finding was inaccurate rather than duplicated: "the rule stated above" pointed at the release-is-proven-on-the-testbed rule, when the sentence following it states the different rule that a caller must exist | The two README paragraphs are cut back to pure pointers, carrying no fact release-flow's README already carries. The subject cannot be corrected in place: the branch is pushed and `git push --force` and `-f` are both denied by `.claude/settings.local.json`, so the fix is the PR title, which is what a squash merge writes — `ci: add the npm ecosystem to the testbed`. The standing lesson is that `[review-before-push]` earns its cost on prose as much as on code: self-review found none of these three. **Done 2026-09-10:** both READMEs are cut back and merged, and `3e2fab9` on `ha-ci-testing` carries the corrected subject | fixed |

### From the first content review of the skill (2026-09-12)

Six weeks of this branch went on CI. These are from reading what the plugin actually ships.

| # | Finding | Fix | Commit |
|---|---|---|---|
| 177 | **The skill's canonical quality-scale rule set is two rules short, and its own freshness contract should have caught it.** `reference/quality-scale.md` lists 52 rules across the four tiers; Home Assistant core carries 54. Missing are `docs-conditions` and `docs-triggers`, both Bronze-tier documentation rules added since the set was captured — an earlier draft of this row said Gold, inferred from the other `docs-*` rules rather than read from hassfest, and row 179 records the reviewer catching it. Verified by diff, not by eye: the `quality_scale.yaml` of three independent Platinum core integrations — `airgradient`, `husqvarna_automower`, `imgw_pib` — each list exactly the same 54, and the set difference against the skill's list is those two and nothing else. An earlier draft of this row said the consequence was a hassfest failure on any repository claiming Gold; that was wrong, and row 180 has the finding it turned into — hassfest never reads a custom integration's `quality_scale.yaml` at all, so the incomplete list fails nothing in CI and is caught only by the skill's own audit, if that audit's list is current. `reference/freshness.md` exists to prevent exactly this and says re-derive any row older than ~3 months; the quality-scale row is dated 2026-06 and was never re-derived, along with two others of the same age | Add both rules to the Bronze list and re-date the row. The deeper finding is that the freshness table is a rule nothing runs: every row in it is a cached fact whose staleness is invisible until something breaks, which is the table's own opening sentence, and three of its five rows are now past their own deadline. A table that depends on somebody remembering to read it has the property it was written to remove. **Done 2026-09-12:** both rules added to Bronze, the row re-dated with the core-yaml method that found the gap, and the HA-minimum-Python row re-derived in the same pass and found unchanged. The table itself is still a rule nothing runs, and that stays open here | `bdc0c65`, `915871b`; the table's enforcement open |
| 178 | **A cached fact written as a comment beside a Dependabot-managed pin is wrong from the first bump.** `templates/requirements.test.txt` carries a version map — `0.13.357 -> homeassistant 2026.8.3` — directly above `pytest-homeassistant-custom-component==0.13.357`. The comment exists because that pin decides which Home Assistant the suite runs against, which is genuinely worth stating. But Dependabot bumps the pin and never the prose. Proven live rather than predicted: `ha-ci-testing` merged its bump to `0.13.364` on 2026-09-10, and its copy now reads `# 0.13.357 -> homeassistant 2026.8.3` immediately above `==0.13.364`, so the line whose whole job is to say which HA you are testing against states the wrong one. Every scaffold carrying this file acquires the same defect on its first bump. Separately the template is itself behind: it pins `0.13.357` (HA 2026.8.3) while PyPI carries `0.13.365` (HA 2026.9.2), so a new scaffold starts a Home Assistant minor in arrears | Stop stating a specific mapping in a file another tool edits. The comment keeps the fact that matters — this pin chooses the HA version, and its Python floor must stay in step — and points at PyPI for the current mapping rather than naming one that will be wrong within the week. The pin moves to the current release in the same change. This is row 98's shape arriving in a copied config rather than a workflow body: the bump moves the value and never the words. **Done 2026-09-12:** pin at `0.13.365`, mapping comment replaced by a pointer at PyPI and one sentence saying why none is written | `e9dab40` |
| 179 | **The fix for row 177 put both rules in the wrong tier, and the file already had a tier wrong before it.** A context-free reviewer over the unpushed commits found it. `docs-conditions` and `docs-triggers` were added to the Gold list; hassfest's `ALL_RULES` assigns both to Bronze, and `airgradient`'s `quality_scale.yaml` lists them under its `# Bronze` block. The tier was inferred from the other `docs-*` rules sitting in Gold rather than read from the source that defines it — the same shortcut row 177 faults the 2026-06 capture for. The consequence this row first stated — a hassfest failure on a `bronze` claim — rested on the same false premise row 180 corrects; the real cost of a wrong tier is that the skill's own audit, once it checks tier completeness, would demand the wrong rules for a claim. Two more from the same read. Pre-existing: the tier table's Gold row said "full test coverage" when `test-coverage` is a Silver rule, in this file's own list three lines below. And the reason the template carries no mapping comment was stated in full in both `freshness.md` and the template, with no pointer either way — the restatement row 176 recorded recurring, recurring again | Both rules moved to Bronze, where the list now holds 20, matching hassfest. "Full test coverage" moved from the Gold row to the Silver row. The reason stays in the template, which ships downstream where `freshness.md` does not exist; the freshness row now points at it. The commit that carried the wrong tier in its subject was rebuilt before push | `bdc0c65`, `915871b`, `e9dab40` |
| 180 | **hassfest never reads a custom integration's `quality_scale.yaml`, and the skill says three things about it that are true only for core.** Read at source: `script/hassfest/quality_scale.py`'s `validate_iqs_file` opens with `if not integration.core: return`, and `Integration.core` in `model.py` is "the path starts with `homeassistant/components`". A custom integration validated by the hassfest action — a Docker run over `/github/workspace` with `--integration-path` — is never core, so the whole plugin returns before opening the file. For a custom integration hassfest checks only the manifest: that `quality_scale` is one of the eight known values (`manifest.py`, the `CUSTOM_INTEGRATION_MANIFEST_SCHEMA`), and that a Silver-or-above claim has a non-empty `codeowners`. Nothing else. So `reference/quality-scale.md`'s "the file must list **every** canonical rule", "claiming a tier makes hassfest enforce it (a single `todo` at/below that tier fails CI)" and "a green hassfest … proves the file is well-formed" each describe core's gate, not the consumer's. The evidence was in plain sight: `ha-lego`, `ha-pimoroni-unicorn` and `ha-ci-testing` all carry 52-rule files, `ha-pimoroni-unicorn` claims `gold` in its manifest, and every one of their hassfest runs is green. The only thing that reads a custom integration's file is the skill's own audit, and that has two gaps of its own: `CANON_RULES` in `skill_audit.py` is the same stale 52 — a consumer `freshness.md` never listed — and `check_quality_scale_and_manifest` checks only that every canonical rule is *present*, never that a claimed tier's rules are `done` or `exempt`. So for a custom integration the tier-completeness rule the skill describes is enforced by nothing at all | Three changes. `quality-scale.md` says what hassfest actually checks for a custom integration and names the audit as the enforcer of the rest. `skill_audit.py` gains the two rules and a tier-completeness check — a manifest claiming a scaled tier with any rule at or below it neither `done` nor `exempt` fails, mirroring the core gate's `SCALE_RULES` walk — with its test written first and seen to fail. `freshness.md`'s quality-scale row names the audit's tier table as a consumer, so the next re-derivation updates both lists in one pass. **Done 2026-09-12.** The tier test was run against the unchanged check and failed on `0 == 1` before the walk was added; a single commit carries both, so git does not show that order. Run against `ha-pimoroni-unicorn`'s real tree the new check reports `manifest claims quality_scale 'gold' but bronze rule(s) are neither done nor exempt: ['docs-conditions', 'docs-triggers']` — the first time anything has said so. The reviewer of the fix found the manifest parse could traceback on a JSON list and `rules:` on a YAML list; both now report rather than crash, with tests | `ha-integration-ci` 2468bc7 on `fix/quality-tier-check`; `b47b8a1` |
| 181 | **The skill demands Platinum typing and its own examples would not pass it; two references are stale from the copy model.** `patterns.md` says every file must pass `python -m pyright custom_components/` with zero errors and that a suppression is a failure, then shows `def __init__(self, hass, device_id: str)` at line 79, `async def async_setup_entry(hass, entry, async_add_entities)` at 91 and 201, and `panels.md` line 22 has `async def async_setup(hass, config)` — none annotated, while the typed-entry example at `patterns.md` 370-372 shows the same signature done properly. A reader copying the notify or description example ships code below the standard the same file sets — though, as the reviewer of the fix noted, not code pyright rejects: the file's own `pyrightconfig.json` example runs `standard` mode, and an unannotated parameter is inferred there, not reported. An earlier draft of this row said the lint step would reject it. Separately, `patterns.md` line 64 says "`scripts/skill_audit.py` fails a repo whose `PLATFORMS`…", naming a path that has not existed in a consumer since row 98 moved the audit to ha-integration-ci; `SKILL.md` line 40 already says where it lives. And line 366 annotates the `type MyConfigEntry = ConfigEntry[…]` alias with `# Python 3.12+ / HA 2024.x`, a floor two years behind the `3.14` the file itself states at line 342 | Annotate every example signature to the standard the file sets, using the line-370 form. Drop the `scripts/` prefix at line 64 and point at `SKILL.md`'s statement of where the audit runs. Replace the stale floor comment with nothing — the alias needs no version note on a repository whose floor is 3.14. Each is a content defect, none is CI. **Done 2026-09-12**, and further than first planned: the reviewer found two more unannotated signatures (`make_notify_handler`, `async_install`) and that the names the new annotations use had no visible import in the notify examples, so both blocks now carry their imports and every signature in the file is annotated | `0cafee4` |
| 182 | **The README describes two skills; the branch ships three.** PR #65's own subject is `feat!: split log triage into its own skill`, and `plugins/ha/skills/ha-triage/SKILL.md` exists with a full description, `plugin.json` and `marketplace.json` both name three skills, and `SKILL.md` of `ha-integration` already routes log reading to `ha-triage`. The README never followed. Read in full on 2026-09-11: its Skills table lists two and says `ha-integration` "also triages a `home-assistant.log`", which the split removed; "Both ship in one `ha` plugin. Both look up…"; the symlink block, the namespaced-command block and the CLAUDE.md snippet each cover two. Six places, one file, all contradicting the branch's headline change — found only because the content review began with the file a user reads first | Add `ha-triage` to the table, drop the triage clause from `ha-integration`'s row, count three, and extend the symlink, command and CLAUDE.md blocks. The CLAUDE.md snippet gains a triage trigger in the same shape as the other two, since the point of that snippet is that Claude reaches for the right skill unasked. **Done 2026-09-12**, all six places | `ee919d9` |

### From the reviews of 2026-09-07, after the gate reached the CI repositories

Four context-free reviewers, one per repository, run once the governance gate covered all
four CI repositories rather than only this one. Every finding below was verified live by
the reviewer that raised it, and every fix re-verified after. The rows here come from
three of the four; the testbed's own review has not reported yet.

Rows 160, 161, 162, 164, 165, 166, 167, 169 and 170 moved to `docs/backlog/2026-09-07-gate-to-ci.md`.

| # | Finding | Fix | Commit |
|---|---|---|---|
| 163 | **This repository's own `.gitignore` has the gap row 156 closed in the template.** It names the Python caches and a dotfile rule and not `node_modules`. Live on 2026-09-07: an untracked Cloudflare wrangler install sits at the root, unrelated to this repository and left by work run from the wrong directory, one `git add .` from being committed. Row 156's fix was scoped to the template and the testbed, so the repository that ships the fix does not carry it. Separately, `docs/workflow-map.md` — deleted on purpose at `f5775ec` — is present again at that path, and not as a file: it is a character device, `1, 3`, the same masking that sits over `.mcp.json`. `git status` reports it untracked and `ruff format` exits non-zero unable to read it. The cause, established on 2026-09-08: the sandbox masks every `denyWrite` path that has no real file behind it, and leaves every one that does. Across the 21 unique such paths in the two settings layers together, exactly two are masked, and they are exactly the two absent from disk. This one is still a live entry in `~/.claude/settings.json`, in both its deny list and its `denyWrite`; `fac5156` removed only the repository's own copy, so the mask is current rather than left over, and an earlier draft of this row had that backwards. The other is `/home/juicebox/ha-panel-ci/scripts`, added on 2026-09-08 for a directory that repository has never had. So a `denyWrite` entry naming a path that is not there manufactures an unreadable file at it | The ignore line is carried, so the install no longer offers itself for staging. The masking entry for `docs/workflow-map.md` was removed from the user-level settings on 2026-09-08 and the device cleared with it, immediately and with no restart: that path no longer exists, `git status` no longer reports it, and `ruff format --check` exits 0 where it had exited 2. The duplicated `/home/juicebox/ha-integration-ci/scripts` entry went with `de6af28`. Left open: `/home/juicebox/ha-panel-ci/scripts` is still listed for a directory that repository has never had, so it still manufactures a phantom there; and the stray `package.json` with its lock file at this repository's root are the maintainer's, since this session created neither and deleting what it did not make is how the wrong thing gets deleted | `a210e3d`, `de6af28`; the panel-ci entry and the stray files open |
| 168 | **Merging this branch would leave three required contexts that nothing can report.** Ruleset 18081788 requires six contexts, and three of them — `CC labelling`, `CC label validation` and `CC title validation` — are unprefixed. This branch turns the workflows producing those three into callers with job ids `pr` and `lint`, and GitHub names a called workflow's check-run `<caller job id> / <called job name>` — so once the branch is on `main` they report as `pr / CC labelling`, `pr / CC label validation` and `lint / CC title validation` instead. PR #65 still shows the unprefixed names, because a `pull_request_target` workflow loads its definition from `main`, which still carries the copied bodies: the check that would catch this is the one thing that cannot see it. Every PR opened after the merge would block forever on three contexts nothing produces. That is the failure rows 138, 141 and 147 each describe from a different angle, arriving on the repository that wrote them | The ruleset and the merge move together: rename those three to their prefixed forms in the same change that merges this branch, and before any other PR is opened. Rename only those three. The other three — `Ruff, Pytest and the skill audit`, `Validate marketplace + plugin manifests` and `Dependency review` — come from ordinary jobs in this repository's own workflows, keep reporting unprefixed, and must stay exactly as they are; an earlier draft of this row said "rewrite to the three prefixed contexts", which read literally deletes three working gates at the moment of merge. A payload built from the live ruleset that changes those three strings and nothing else is at `.tmp/ruleset-ha-skills-after-merge.json`. It cannot be applied earlier, because the prefixed contexts do not exist until the callers are on `main`, and requiring a context that has never reported blocks every PR in the meantime | open |
| 171 | **The register drifted again within a day, and one of its own remedies was destructive.** A fifth review found it. Rows 158 and 162 and the Open section all still said "unpushed" for branches already on GitHub — the second prose version of that index, drifting exactly as the first had. Row 168's remedy read "rewrite 18081788 to the three prefixed contexts", which carried out literally deletes the three required contexts this repository's own ordinary jobs produce, stripping three working gates at the moment of merge. Row 163 said it had checked six paths where the two settings layers hold 21, and had the cause backwards: `docs/workflow-map.md` is still a live entry in the user-level settings, so its mask is current rather than residual, and the remedy pointed at the wrong file. Row 166 claimed three places now say why the previous window counts when only the docstring does. Four already-cleared rows — 105, 108, 114 and 115 — carry a fifth cell that GitHub-flavoured markdown silently drops, the same defect fixed in three others the day before. And two subjects overstated their diffs: `f9a6331` says three rows and changed two, `9539148` says the template carries a comment it does not | All corrected. The Open section is no longer prose: it is a table with one state column per row, so there is a single place to keep true rather than a paragraph that restates what the Commit column already says. The four fifth cells are merged into their Fix columns. The two subjects stand, since they are committed and the rows they describe are right. Still open and the maintainer's: the restored `[read-the-file]` hook and the surviving `[read-dont-search]` hook state the same three facts and both fire every session | part fixed, part open |

### From the CI contract of 2026-08-21, reconciled 2026-09-04

The contract's gap list lived outside this repo, and the CI plan of 2026-08-31 was built
from this file alone, so its item 8 was never planned and the copy model it replaces was
audited and fixed for three days instead. Everything still outstanding from that list is
here now, so there is one list.

Row 99 moved to `docs/backlog/2026-08-21-ci-contract.md`.

| # | Finding | Fix | Commit |
|---|---|---|---|
| 98 | **Workflow bodies do not propagate to scaffolds.** Dependabot bumps the action pins inside a copied workflow and never its body, so the setup-python step of row 84, the panel guard of row 85 and every future change reach a scaffold only by hand-copying the templates, as `ha-ci-testing` #12 and #13 did. Contract item 8, agreed 2026-08-21, was never planned. **Aim:** a change to integration CI reaches every scaffold without anyone copying anything, and every scaffold can state which CI version it runs. **Goals:** (1) each workflow has one body in one home, and a scaffold's workflow file is a pointer to it, `uses: <repo>/.github/workflows/<name>.yml@<sha> # vX.Y.Z`, the shape every popular action already uses; (2) the body checks the CI repo out at its own SHA to run `scripts/`, so scripts, their tests, `patch_twins`, the byte-identical checks and `check_self_diff` all go; (3) no secret crosses except the draft-PR opener's `RELEASE_TOKEN`, declared required by that one workflow and supplied by name in its pointer, never inherited; the other eleven use the scaffold's own `GITHUB_TOKEN`; (4) Dependabot delivers a CI release to every scaffold as its existing weekly grouped PR, moving the SHA and the version comment together; (5) the version is a repo's release tag, pinned as `@<sha> # vX.Y.Z`, because GitHub versions repositories, not files, and the split below is by shared code so that a bump moves only the pieces that share it; (6) no repo runs integration workflows on itself; the testbed is where a release of any of them is proven before it is tagged; (7) what a scaffold still copies, the pointer files, `dependabot.yml`, the drafter config, `ruleset.json`, the commit hook, the `CLAUDE.md` snippet, `pyproject.toml`, `conftest.py`, `requirements.test.txt`, is compared by the audit against the templates at the pinned version and named on drift; (8) the audit otherwise judges the integration and the repo's GitHub side, nothing about copies. **Goal (7) struck 2026-09-06, re-decided 2026-09-10 and the strike stands:** row 173 carries the reasoning and the measurements. **Decided 2026-09-06:** ha-skills becomes a plain consumer too: four release-flow callers and its own `ci.yml`; its copied bodies, the five release scripts, their tests and both `templates/` mirrors go, and `patch_twins` with them. **Decided 2026-09-04, repos created:** `release-flow` holds `pr-checks`, `lint_pr`, the draft-PR opener, the release drafter, the four scripts they share through the commit classifier, the drafter config template and the commit hook, generic to any repo using Conventional Commits; `ha-integration-ci` holds `python_validate`, `release` and `quality_audit` with `skill_audit.py` and `version_sync.py`, the three that encode what a validated integration is; `ha-panel-ci` holds `panel_bundle` and the `frontend/` templates; `ha-skills`, this repo renamed, holds the skills and the copied files only, so a skill release moves no pointer. The four workflows that are settings over a third-party action, stale, dependency review, HACS and hassfest, stay as plain files in the scaffold pointing at the action directly; they carry nothing worth versioning. **Done when:** the testbed carries pointers only and its full cycle is green; one CI release is observed arriving at the testbed as a Dependabot PR and going green with no hand edit; `templates/` holds no workflow body and no script; the copy-model checks are deleted, not kept; row 99 has removed every sentence describing copying. **Scope corrected 2026-09-11:** "`ha-lego` and `ha-pimoroni-unicorn` are migrated the same way" stood in that list and is removed from it. Migrating a real integration is the skill being *used*; this row is the architecture being *built*, and the testbed exists so that adoption is never what proves the model — the first two criteria are that proof, and both are met. Conflating the two made this row read as blocked on work outside itself and made the branch look unfinishable until two unrelated repositories moved. That work is each repository's own `docs/ci-migration-todo.md`, and it gates neither this row nor the branch. **Standing 2026-09-06:** the testbed carries callers only and its cycle is green (`ha-ci-testing` #14-#16); `templates/` holds no body and no script; the copy-model checks are gone; this repo calls release-flow (63df160, ad0ed5d, 18c6721); row 99 is cleared; `patch_twins` is out of the gate (a83cd76). The label gate was re-proven under the callers on the testbed the same evening: PR #9, titled `fix:` over one `feat!:` commit, went `pr / CC label validation` FAILURE with the comment naming `feat!:`, beside `lint / CC title validation` and `pr / CC labelling` green. Goal (4) is half proven: the testbed's dependency graph lists all seven reusable workflows it calls as `github-actions` dependencies at their pinned SHAs, so Dependabot sees them; what has not happened is the bump PR, and the reason is in the updater's own log (`ha-ci-testing` run 34062514133, and the same on the other two): it reads `Checking if PineappleEmperor/release-flow/.github/workflows/auto-draft-pr.yml 1.0.0 needs updating`, then `Available release version/ref is 1.0.1`, then `Days since release : 0 (cooldown days 3)` and `All versions are in cooldown period, returning current version`. Dependabot resolves the SHA pin to its tag, sees `v1.0.1`, and holds it for three days by default. So every step of goal (4) is proven except the PR itself, which is a matter of waiting until 2026-09-09 or setting `cooldown: {default-days: 0}`; ha-integration-ci's README now states the delay. The testbed's `github-actions` interval moves to `daily` separately (`ha-ci-testing` PR #17, green, awaiting merge). **Goal (4) proven 2026-09-09**, the day the cooldown expired: the bump arrived as `ha-ci-testing` PR #20, one grouped `actions` PR moving all four release-flow callers from `v1.0.0` to `v1.0.1`, with the SHA and the version comment changing together, `91c33e4` to `8b41d48`. Read live on 2026-09-10 its merge state is `CLEAN` against all eight required contexts with no hand edit, which is this row's own *done when* criterion for that goal. Two of the eight report as skipped — `pr / CC label validation`, which release-flow's own `if:` exempts bot PRs from, and `draft / Auto draft PR` — and GitHub counts a skipped required check as satisfied, so the exemption does not leave a Dependabot PR blocked. Merged 2026-09-10 at 07:48, and the first runs under the new pin were green: `Release Drafter`, which is release-flow `v1.0.1` executing through a caller, alongside `Skill Audit`, `Hassfest` and `Python Validate`. The drafter resolved `v0.3.1` from the merged labels and kept a full draft and an rc draft alive together, one patch above the last full release `v0.3.0`, which is the behaviour its README describes. So goal (4) is proven end to end rather than in halves: a CI release reached a consumer as its ordinary grouped Dependabot PR, went green, merged with no hand edit, and the version it delivered then ran. **Standing 2026-09-11:** every criterion under *Done when* is met, so what remains before this row closes is the branch that carries it. PR #65 is gated on two things that are not this row's work: its README still describes two skills where the branch ships three, and the content of the skills themselves has not been revisited since the CI rebuild began — weeks of this branch have gone on plumbing, and the guidance the plugin actually ships has not been read in that time. Neither is a CI problem. The merge also resolves rows 168 and 172 | open, resolves at the PR #65 merge | — |

### From the independent review of rows 84-90 (2026-09-04)

Moved to `docs/backlog/2026-09-04-review-84-90.md`. Rows 91 to 97.

### From the testbed run (2026-09-04)

Moved to `docs/backlog/2026-09-04-testbed-run.md`. Rows 85 to 90.

### From the ruff survey (2026-09-04) — cleared 2026-09-04

Moved to `docs/backlog/2026-09-04-ruff-survey.md`. Rows 82 to 84.

### From the gate rebuild (2026-09-03) — cleared 2026-09-04

Moved to `docs/backlog/2026-09-03-gate-rebuild.md`. Rows 79 to 81.

### From the CI audit pass (2026-08-31) — cleared 2026-09-03

Moved to `docs/backlog/2026-08-31-ci-audit.md`. Rows 73 to 78.

### From the post-fix independent audit (2026-08-26) — all fixed

Moved to `docs/backlog/2026-08-26-post-fix-audit.md`. Rows 44 to 72.

## Fixed

Moved to `docs/backlog/2026-08-25-earlier-fixes.md`.
