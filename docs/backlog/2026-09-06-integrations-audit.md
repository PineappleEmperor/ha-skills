# 2026-09-06, the audit of the two integrations

Closed rows, moved here from the register when they cleared. Formatting may be repaired; a
claim later found false gets a new row in the register rather than an edit here, so this
stays a transcript that can be checked against git.

| # | Finding | Fix | Commit |
|---|---|---|---|
| 141 | **The audit calls a matrix job's required context unproducible.** `check_required_contexts_have_producers` maps a job to `name or id` through `_contexts`, and GitHub suffixes a matrix job's check-run with its values, so `ha-lego`'s ruleset requiring `lint-and-type (3.14)` — which its `python_validate.yml` produces from `strategy.matrix.python-version: ["3.14"]` — is reported twice as a context that "can never report and every PR stays blocked". The repository has merged PRs through that check. The advice the failure gives is to drop the context, which would remove a working gate; the check exists to catch exactly the opposite mistake. `github-setup.md` already states the rule the code does not implement: "Never assume a context equals the job name: a matrix suffixes it" | `_contexts` expands a `strategy.matrix` into the names GitHub generates, and falls back to accepting the `<name> (` prefix when `include`/`exclude` make the product unknowable. A test per shape. Done as stated, four tests; re-run against `ha-lego` the failure is gone and the ruleset reads as healthy | `ha-integration-ci` 3401081, on `fix/audit-false-failures`, PR #1 green and awaiting review |
| 142 | **The drafter-category check states a consequence that is false.** `check_drafter_categories` fails a `categories[*].labels` with "v7 matches under `when:` and these never match, so the version resolves to a patch bump". Read at `release-drafter/release-drafter` `v7.7.0`: `schema.json` still declares `labels` and `label` beside `when`, and `src/actions/drafter/config/parse-categories.ts` merges those `deprecatedLabels` into every `when` condition, synthesising `[{}]` when a category has no `when` at all. It warns and links a migration note; it does not stop matching. Both integrations therefore resolve their versions correctly today, and the row-105 fix that moved this repository's own config to `when:` was right for a different reason — the field is deprecated and will be removed | Keep the check, since the field is going away, and say what is true: the form is deprecated, the action warns on it, and it is removed in a future release. A test asserting the message names deprecation rather than a bump. Done as stated | `ha-integration-ci` 0db3948, same branch and PR as row 141 |
| 145 | **ha-panel-ci's `panel-bundle.yml` only warns where `ha-lego`'s `frontend_build.yml` fails.** `ha-lego` ends its build with `git diff --exit-code custom_components/lego/panel/lego-panel.js`, so a committed bundle that no longer matches its source blocks the merge; the reusable workflow emits `::warning::the committed panel bundle is stale` and passes. HACS ships the repository as-is, so the stale bundle is what users run. Migrating `ha-lego` as-is would trade a blocking guard for a warning | Decided 2026-09-07: the shared workflow gains an opt-in failing mode. `panel-bundle.yml` takes a `fail-on-stale-bundle` input, default false; a consumer that wants red rather than a warning passes it in its caller. The check stays advisory either way | `ha-panel-ci` e870bec, on `feat/fail-on-stale-bundle`, pushed as PR #2 |
| 146 | **Seven workflows across the two repositories have no home in the three-repository stack.** Both carry `semantic_release.yml`, which publishes a release on a `v*.*.*` tag push with GitHub's own notes, which the drafter's `release: published` path would then overwrite; the two copies differ (31 lines against 35). `ha-lego` also has `dependabot_automerge.yml` and `live_api_tests.yml`; `ha-pimoroni-unicorn` also has `cut_rc.yml`, `firmware_test.yml` and `check-engine-version.yml`. None is a copy of anything the CI repositories own, so the migration cannot be mechanical: each is kept as a repo-specific workflow, promoted into a CI repository, or deleted | Decided 2026-09-07: not this repository's work, and not this PR's. Each integration gets a `docs/ci-migration-todo.md` listing what its migration must do, written and left in place; nothing else there is touched until the model this PR defines has landed | `ha-lego` and `ha-pimoroni-unicorn`, uncommitted in each working copy |
| 147 | **The three CI repositories protect nothing.** `gh api repos/PineappleEmperor/<repo>/rulesets` returns `[]` for `release-flow`, `ha-integration-ci` and `ha-panel-ci`, and `branches/main/protection` 404s on each. The PR gates row 137 gave them are therefore advisory, `main` can be force-pushed or deleted, and a release is tagged from an unprotected branch — in the three repositories whose `main` defines every consumer's CI. `ha-skills`, `ha-ci-testing` and `ha-lego` each carry `deletion`, `non_fast_forward` and `required_status_checks` with an empty bypass list | Apply the same three rules to each. The contexts are known and stable, unlike row 143's: `pr / CC labelling`, `pr / CC label validation`, `lint / CC title validation` in all three, plus `Ruff and Pytest` in release-flow and `Ruff, format and pytest` in ha-integration-ci; ha-panel-ci runs no suite of its own. Payloads written to `.tmp/ruleset-<repo>.json`, each a `gh api -X POST repos/PineappleEmperor/<repo>/rulesets --input` away. Applied by the maintainer 2026-09-07; all three now return "Protect main active" | maintainer step, done |
