# 2026-09-06 to 2026-09-07, proving the panel path

Closed rows, moved here from the register when they cleared. Formatting may be repaired; a
claim later found false gets a new row in the register rather than an edit here, so this
stays a transcript that can be checked against git.

| # | Finding | Fix | Commit |
|---|---|---|---|
| 148 | **ha-panel-ci's only reusable workflow has never been called.** No repository carries a `panel-bundle.yml` caller: the testbed has no `frontend/` and does not call it, `ha-lego` and `ha-pimoroni-unicorn` still carry their own `frontend_build.yml`, and ha-panel-ci does not call its own. So `v1.0.0` of that repository is untested against goal (6) of row 98 — a release proven on the testbed before it is tagged — and `ha-lego`'s migration would be its first live run, on a real integration rather than the testbed. The workflow's own README explains a first-run failure it already had at `setup-node`'s cache step, which is the class of thing a testbed run exists to catch | Decided 2026-09-07: the testbed gets its own panel — copying `ha-lego`'s for inspiration only, since that repository is not a test bed. Done: `frontend/` from ha-panel-ci's templates with a Lit component, a unit test and the committed bundle, plus the caller. `npm run check` caught two missing `override` modifiers under the template's `noImplicitOverride`, which is the strictness earning its keep. Proven: PR #18's first `panel / Panel type-check and tests` failed, on `npm ci`, and it now passes — the failure it caught is row 157 | `ha-ci-testing` d9f3c1c, 2a33c77, 6846292 |
| 150 | **The row 134 pin can lower a release below the increment its labels demand, and this repository is exposed right now.** `draft_version.py` returns the base of the most recently published release whenever that release is a prerelease, and `release-drafter.yml` passes it as the drafter's `version:` input, which overrides the label-resolved version outright. So while an rc line is open every later merge is drafted at that line's base, whatever it contains. This repository's newest published release is `v7.2.1rc1` (2026-08-18), so the pin returns `7.2.1`; PR #65 carries `refactor!: drop the version gate that gated nothing` and `feat!: gate merges on the label matching the commits`, and merging it would draft `v7.2.1` for two breaking changes. That is the failure the label gate exists to stop, arriving by another route. release-flow's README says the labels "take over again once a full release exists", which is not what the code does — `test_an_rc_line_after_a_final_pins_the_rc_base` fixes the current behaviour deliberately | A decision on release-flow, not a quick fix. Three shapes: pin only when no full release exists at all, which is the literal case row 134 found; or have the pin step resolve the labels since the last full release itself and take the higher of the two; or keep it and make an open rc line something a breaking change may not be merged into, enforced somewhere. Decided 2026-09-07: the first, and the other two were not real alternatives. The pin exists for the one case release-drafter cannot resolve at all; every other case is already handled by resolving from the last full release, which is what carries a `7.2.1rc1` line to `7.3.0rc1` and on to `8.0.0rc1` as the merged work grows. Done: `pinned_version` returns nothing when any full release appears in the list, the workflow's window widens from 20 releases to 100 so a long candidate run cannot hide the full release behind it, and the two tests that encoded the old behaviour now assert the new one. Against this repository's live release list the pin returns nothing, so the labels decide | `release-flow` f44121e, on `fix/pin-only-without-a-full-release`, pushed as PR #2 |
| 151 | **Seven comments and docstrings on this branch describe files the branch deleted.** A context-free reviewer over the whole of PR #65 found them. `pyproject.toml:1` says "scripts/ and tests/ ship byte-for-byte to every scaffolded integration", which was true of the five release scripts and is false of the five that remain — `skill_meta_audit.py`'s own docstring says the opposite, that shipping it would be dead weight. `scripts/skill_meta_audit.py:11` says it runs in `quality_audit.yml`, deleted by this branch; `ci.yml` runs it. `.github/workflows/validate-manifests.yml:20` cites `skill_audit.py`, which this repository no longer holds. `plugins/ha/skills/ha-integration/conftest.py:9-11` tells the reader to build a fixture for `templates/tests/test_manifest_gate.py`, and neither the test nor the fixture path exists. `templates/.github/workflows/hacs-validate.yml:3` and `hassfest-validate.yml:3` point at `reference/github-actions.md` for the one fact a reader of those files wants — why they are pinned to `@main` and `@master` — which lives in `reference/freshness.md` | Correct each to what is true now. Done as stated, one commit each. Five of the seven were a path into the skill's own tree, and nothing judged those — `check_docs_match_templates` reads workflow names only — so the meta audit gains `check_named_paths_exist`: a `templates/`, `reference/` or `evals/` path named anywhere under a skill, backticked in prose or bare in a shell string, must resolve against some skill in the repository. Run against the tree as it stood before these fixes it reports all six stale paths, including the three in `make_fixture.sh` that row 152 found by hand; against the tree now it reports none. `docs/`, superseded passages and `evals/results/` are excluded, each because it names a deleted file on purpose | efc6c46, 6507e10, bdd98b1, 91cd583, 6bd0da9, 3be84e8 |
| 152 | **Two of the six eval fixtures cannot be built.** `evals/make_fixture.sh 02` copies `templates/scripts/skill_audit.sh`, `templates/scripts/manifest_gate.py` and `templates/tests/test_manifest_gate.py`, and loops over eight `templates/.github/workflows/*.yml`; `make_fixture.sh 03` copies `templates/.github/workflows/python_validate.yml`. None of those paths survives the caller model, so both scripts exit non-zero today. Scenario 02 is the deeper problem: its premise is a *paraphrased workflow body*, and the seven caller workflows have no body to paraphrase, so the failure it guards now only reaches the four plain workflows and the copied configs. Its green-gate step also names `scripts/skill_audit.sh`, superseded by ha-integration-ci's `skill_audit.py`, which a fixture can only run from a checkout of that repository | Scenario 03 rebuilds against a `python-validate.yml` caller. Scenario 02 keeps its lesson — diff the copies, never trust a green gate — with the drift planted in `hacs-validate.yml`, which is still copied whole, and a second planted drift in a caller (a hand-written `pr-checks.yml` with the right `uses:` and the wrong `permissions:`), which is the new shape of the same mistake. Done, with one change from that plan: scenario 02's fixture is now a history-stripped clone of the testbed rather than a synthetic tree, because the premise is a repo that passes the gate and hand-building one means enumerating 52 quality-scale rules and a brand icon, then re-enumerating them whenever the audit grows a check. The gate reports the caller drift and says nothing about the `hacs-validate.yml` one, so the scenario now has a hard arm and an easy one; all three fixtures build | d1de542 |
| 153 | **`templates/frontend/` is a second copy of files ha-panel-ci owns.** `package.json` and `tsconfig.json` under `templates/frontend/` are byte-identical to `/home/juicebox/ha-panel-ci/frontend/`'s, and `reference/github-actions.md` already tells a scaffold to take them from ha-panel-ci — its *Where `templates/` lives* list does not mention `frontend/` at all. So the skill carries a duplicate of a file another repository is the source for, which is the exact shape row 98 exists to remove, and nothing keeps the two in step once ha-panel-ci bumps a dependency range | Delete `templates/frontend/`. `git rm` on a governed path is refused to this session, so it is a maintainer step; the reference table already points at ha-panel-ci and needs no edit. Done by the maintainer 2026-09-07; the directory is gone from the index | 951aec2 |
| 154 | **Six commit subjects on this branch join two changes with "and".** `b8408c4` clears one row and holds two others; `f18c224` clears three rows and records a rename; `0ccbf9c` adds a lint check and a format check; `30ba147` blocks shell search and demands files be read; `fda8c3e` merges two sections and corrects their claims; `1c717c6` restores a discipline doc and corrects three contradictions. Found by the whole-PR reviewer against the rule `.githooks/commit-msg` now enforces | `wontfix`: the hook that rejects the shape was added mid-branch and every subject since obeys it, so the six are all older than the rule. Fixing them means rewriting 265 commits of a branch that is under review, which trades a real risk for a cosmetic one | wontfix |
| 155 | **The new path check excused more than it meant to, and would have failed four things it should not.** A context-free reviewer over `3be84e8` found all four by construction. `"results" in doc.parts` excused any directory named `results` anywhere in a skill, so a stale path inside `reference/results/` went unreported. `DOCS_EXCUSED` matched against the nearest heading of any level, so a document titled "Removed features and what replaced them" silenced every line under it — a whole file, from its `#` title. And the pattern is unanchored: a path inside a fenced example, and one inside a URL to another project (`…/blob/main/templates/scripts/theirs.py`), both read as claims about this tree. None occurred live, which is why only a reviewer building the cases found them | Excuse `evals/results/` by adjacency rather than by name; let only a `##` or deeper heading excuse its body, and clear the section at a `#` title; skip fenced lines and any line carrying `://`. A test per case, each failing first. Re-run against the pre-fix tree it still reports all six real stale paths, so none of this narrowed the catch | b2636c0 |
| 156 | **`templates/.gitignore` does not ignore `node_modules/`.** Found by running the first `npm install` in the testbed's new `frontend/`: git offered the whole installed tree for staging. Measured later at 79M, not the "several hundred megabytes" this row first asserted without measuring. `ha-lego` added the line by hand under a "# This repo" heading, so every panel repo scaffolded from the template has to notice this for itself | Add it to the template, and to the testbed's copy | b655a77; `ha-ci-testing` d9f3c1c |
| 157 | **The panel template's `esbuild` range disagrees with the one `vitest` pulls, and `npm ci` then fails on a Linux runner.** ha-panel-ci's `frontend/package.json` asks for `esbuild: ^0.25.0` while `vitest: ^4.0.0` depends on `0.28.x`, so npm resolves two copies; the nested one loses the `optional` flag on esbuild's platform packages, and `npm ci` tries to install `@esbuild/aix-ppc64` and exits `EBADPLATFORM`. It cannot be seen locally: the install that writes the faulty lock file succeeds. Found by the first run row 148 arranged — `panel / Panel type-check and tests` failed at Install on `ha-ci-testing` PR #18 | Pin `esbuild` to the major vitest resolves, and say in ha-panel-ci's README that the two move together. The testbed's own `package.json` and lock file with it; `npm ci`, `check`, `test` and `build` all pass, and the check went green | `ha-panel-ci` fbd10ac; `ha-ci-testing` 6846292 |
