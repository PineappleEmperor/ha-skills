# 2026-09-07, the reviews after the gate reached the CI repositories

Closed rows, moved here from the register when they cleared. Formatting may be repaired; a
claim later found false gets a new row in the register rather than an edit here, so this
stays a transcript that can be checked against git.

| # | Finding | Fix | Commit |
|---|---|---|---|
| 160 | **The coverage check row 158 built could be defeated three ways, and its prose stated three things that were false.** A quoted `uses:` value did not match, so a workflow that *is* called would be reported as never called — the worst verdict the check can reach. A `uses:` line inside a YAML comment did match, so a workflow that is *not* called would pass. A trigger written `on: [workflow_call]` was not recognised at all, so such a workflow was dropped from the list and never judged. In release-flow's README: the token paragraph said "the other three workflows" where there are five; the list of what its tests cover omitted the new test; and nothing told a local reader that the code needs an interpreter no older than the one CI installs | Each defeat pinned as a failing test first, then fixed: quotes optional in the pattern, comments stripped before matching, and the mapping, list and bare-string forms of `on:` all accepted. The three prose claims corrected against the file rather than against memory | `release-flow` 22f5180, 967d09a, 5a7bef0 |
| 161 | **The same branch would have turned release-flow's CI red, and the check it adds had no caller anywhere.** `testbed_coverage.py` imports `yaml`, the first third-party import in that repository, and `requirements.test.txt` did not carry it — so in a clean environment built from that file `pytest` aborts at collection and *none* of the 111 tests run, not merely the new ones. It passed locally only because pyyaml happens to be installed in the ambient interpreter, which is the same mistake as row 159's `npm install`: running something adjacent to what CI runs. Three more defects behind it: nothing called the new reusable workflow, so a branch enforcing "a workflow with no caller has never run" shipped a workflow with no caller; the reader step piped `gh api` into `base64` under the default `bash -e`, which has no `pipefail`, so a failed API call was masked and the check could pass on shrunken evidence; and a `.yml` parsing to a scalar crashed the run rather than being skipped. The README also justified its Python floor by pointing at a ruff lint setting, which is not a runtime requirement | pyyaml pinned in the one requirements file every job installs from, and the workflow now installs from that file rather than an unpinned inline name. The reader step sets `pipefail`. A document that is not a mapping is skipped, pinned by a test that crashed first. release-flow calls the check from its own `ci.yml`, rather than from a caller file of the same name that would collide with the workflow it calls. Proven: a venv built only from the requirements file runs 112 tests green, and the check run by hand against the live testbed reports full coverage | `release-flow` 03533f6, 4c70371, 527b7b3, e3dd29d, 29cf39c |
| 162 | **ha-panel-ci's README misdescribes the branch that changes it, and PR #2's green checks prove none of it.** The "What is here" table still says the workflow has "no inputs" while the branch adds one. *Proving a panel locally* was inserted before the paragraph that closed the section above it, so the four sentences explaining what `fail-on-stale-bundle` does now sit under a heading about running commands locally — the one section a reader looking for the input will not open, and the same placement error an earlier edit was already flagged for. Its command block never says `cd frontend`, so run verbatim from a checkout the first command exits 1. It states that `npm install` succeeds, which is false on the node and npm the workflow itself pins: there it exits 1 in `vitest`'s own resolution. And "esbuild is pinned to the major that vitest depends on" is wrong twice — nothing declares esbuild as a dependency, `vite` declares it as a peer with a two-major range, and 0.28 is a minor. The input's effect is stated in three places and "never a required context" in three more. Nothing in PR #2 ran `npm ci`, a build, or the stale-bundle step: its four green checks are title, label and draft lint | Correct the table, move the local-run section back below the paragraph it split, add the missing `cd frontend`, state the npm claim against the toolchain the workflow actually pins, and describe the constraint as `vite`'s peer range rather than as a vitest dependency. State the input's effect once and point at it from the other two places. None of it is proven until something runs `npm ci` against the branch. All six corrected, over three commits, because the first attempt got two of them wrong and a second review caught it. Right first time: the table names the input, the `with:` block follows the placeholder instructions rather than splitting them, the local-run section sits below the section it was cutting in half and opens with `cd frontend`, and the npm claim is stated against both toolchains. Wrong first time: the esbuild paragraph asserted that nothing declares esbuild directly, which `frontend/package.json` flatly contradicts, and the three statements of "never a required context" were left standing while only the input's effect was deduplicated. Both now fixed — a direct pin that must satisfy vite's peer range, and one statement each with the workflow's input description pointing at the README. Still true and recorded rather than fixed: no run has exercised any of it | `ha-panel-ci` 0b890d4, 91fa94c, 597b9fb, pushed to PR #2 |
| 165 | **Four scratch clones were deleted while a reviewer was still reading one.** The recovery into the real repositories was complete and verified first, so nothing was lost, but the running reviewer lost the checkout under it and had to reconstruct two line numbers from diff hunk headers instead of reading them live. It also looked, from outside, like work vanishing on its own | Commit anything worth keeping before deleting anything, then wait for a running agent's completion notification or delete only paths its brief never named. Kept as a rule rather than a code change, since nothing can see another agent's open file handles | rule, not code |
| 166 | **The gate's headline change was unproven, and two of its own messages described a stricter rule than its code.** The environment-driven root and profile that let one gate serve four repositories were asserted only through `profile()` mapping two names, with a `monkeypatch` argument the test never used; put the hardcoded root and map back and the whole suite still passed, so `56f2091` rested on nothing. Separately the claim check's refusal and row 89 both said "this hour", and its docstring said "in the current window", while the code accepts a read from the previous rotation window too — a deliberate tolerance that was documented nowhere and pinned by nothing. And an unknown `GOVERNANCE_PROFILE` raised a `GateError` while the module was still importing, which contradicts that class's own stated contract of reaching the caller as a tool error and never as a crash | The test now executes the module afresh with both variables set and asserts the root and the tier map they produce, and asserts the defaults with them unset; against a copy with the two lookups removed it fails on the root. The refusal and row 89 now say what the code does, and the docstring is the single place that says why the previous window counts. A mistyped profile stops the server with the one line naming the fix, rather than a traceback. One deviation to record: all three landed in a single commit, because they share two files and could not be separated after the fact | bc7797c |
| 167 | **A branch checkout silently leaves a governed file holding the other branch's content.** Switching ha-panel-ci to `feat/fail-on-stale-bundle` moved every ungoverned file and left the two governed ones at `main`'s content, reported afterwards as ordinary local modifications. The sandbox denies writes to those paths, so git could not replace them; it printed the two names and carried on. `git restore --source=HEAD` then failed with "unable to unlink old … Read-only file system" and "Device or resource busy", and `git checkout main` refused to move back over modifications it had created itself. The tree sat on the branch holding the other branch's files, one `git commit -a` away from reverting the branch's work, and nothing in any of that output said so | Recovered by writing the branch's committed content back through the gate, patch by patch, until `git status` agreed. The gate is doing exactly what it is for; the hazard is that git reports its refusal as a local edit rather than as a failure. So: one branch per repository for as long as the gate is on, switching done from a shell the sandbox does not cover, and `git status` read immediately after any checkout that touches a governed path | rule, not code |
| 169 | **A guard was deleted inside a config change committed without its diff being read.** `fac5156` was made to put the four CI repositories' governed paths into this repository's `.claude/settings.json`, and it does that. It also removed the `[read-the-file]` prompt hook outright. That removal was not made from this session: the sandbox denies writes to that file, and an append to it returns `Read-only file system`, checked again on 2026-09-08. The file was edited outside the session and then staged here unread, which is the whole of this row's failure. What it removed was the only place stating that a file is read in full before anything is said about it, and that a python or awk line-scanner over source is the blocked shell search wearing a hat. The surviving `[read-dont-search]` hook fires on the `Grep` tool alone, so nothing now says it about python, which is the exact habit that hook was added to stop. The same commit also dropped `patch_twins` from the governed-edits text and `docs/workflow-map.md` from the repository's two deny lists but not from the user-level ones, which is why row 163's mask is still in force; both removals are right in themselves, since the twins and that file are gone, but all three arrived together in a change nobody read. Separately the new lists omit `/home/juicebox/ha-ci-testing/scripts`, which the `ci` profile governs and which the new `allowWrite` entry makes writable — a probe file created and removed there through Bash confirms a governed tier is writable outside the gate | Restore the hook and add the two missing deny entries; both are maintainer steps, because the sandbox denies writes to that settings file itself. The general lesson is the one this row is an instance of: committing somebody else's configuration still means reading the diff first, and a `git add` of a file this session did not write is the moment to do it. Done by the maintainer on 2026-09-08 and committed here after the diff was read: the hook is back and firing, and a probe under the testbed's `scripts` now returns `Read-only file system` where it used to succeed | 92ba4d7 |
